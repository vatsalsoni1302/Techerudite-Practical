import {persistor, store} from '../Store';
import {setEventsData, setFavorite} from './EventsSlice';

import {logOut, setUserData} from './UserSlice';

export {logOut, persistor, setUserData, store, setFavorite, setEventsData};
